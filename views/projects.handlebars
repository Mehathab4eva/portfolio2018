<div class="projects">

	{{#each data}}

	<a href={{this.link}} target="_blank">
			<div class="eachProject">
					<img class="projectPic" src={{this.imgURL}} />
					<h2 class="projectName">{{this.name}}</h2>
			</div>		
	</a>
	{{/each}}

	
</div>